#include <stdio.h>

int main() {
	int a, b, c;
	int max;
	scanf("%d %d %d", &a, &b, &c);
	if (a > b)
	{
		if (a >= c)
		{
			max = a;
		}
		else if (a < c)
		{
			max = c;
		}
		else
		{
			printf("없음");
		}

	}
	else if (a <= b)
	{
		if (b > c)
		{
			max = b;
		}
		else if (b < c)
		{
			max = c;
		}
		else
		{
			printf("없음");
		}
	}
	printf("%d", max);
	return 0;
}
