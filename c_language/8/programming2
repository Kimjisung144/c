#include <stdio.h>
#include <math.h>

void check_alpha(int alpa)
{
	int count = 0;
	for (int i = 97; i <= 122; i++)
	{
		if (i == alpa)
		{
			printf("%c은/는 알파벳입니다.", alpa);
			count++;
		}
	}
	if (count != 1)
	{
		printf("%c은/는 알파벳이 아닙니다.", alpa);
	}
	return;
}
