//int형의 변수 x와 y의 값을 서로 교환하는 츠로그램을 작성하여 보자. 별도의 변수가 필요하면 정의하여서 사용한다.변수 x와 y는 10,20의 값으로 초기화하라.
#include <stdio.h>
int main()
{
	int a, b,sum;
	a = 20;
	b = 30;
	printf("a는 %d b = %d\n", a, b);
	sum = a;
	a = b;
	b = sum;
	printf("a는 %d b = %d",a,b);
	return 0;
} 
