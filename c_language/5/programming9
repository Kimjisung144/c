//그리스 최초의 수학자이자 천문학자인 탈레스는 지팡이 하나로 피라미드의 높이를 재었다고 한다. 탈레스는 지팡이를 똑바로 땅에 세우고지팡이를 
//움직여서 지팡이의 그림자와 피라미드의 그림자를 일치시켰다. 삼각형 abc와 삼각형 ade는 닮음꼴이므로 다음의 수식이 성립한다.
#include <stdio.h>
int main()
{
	int BC, AC, AE;
	float DE;
	printf("지팡이의 높이,지팡이의 그림자길이,피라미드까지의 거리를 입력하시오:"); // 
	scanf_s("%d %d %d", &BC,&AC,&AE); //각각 입력
	DE = (AE * BC) / (float)AC; // AC:AE = BC:DE 먼저 AE와 BC를 곱한다음 그값과 AC*DE과 같기에 AC를 나누면 DE값이 나온다.
	printf("피라미드의 높이는 %f 입니다.",DE); // 
	return 0;
}
