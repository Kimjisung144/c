//구표면적과 체적을 구하는 프로그램을 작성하라. 구의 반지름은 실수로 입력된다. 아래의 공식을 사용하라.파이값은 기호상수를 사용하여 정의하라
#include <stdio.h>
#define pi 3.141592
int main()
{
	float r;
	printf("구의 반지름을 입력하시오:___\b\b\b"); // 
	scanf_s("%f", &r); // 구의 반지름 입력
	printf("표면적은 %f\n체적은 %f",4*pi*(r*r), (4.0/3.0)*pi*(r*r*r)); // 공식을 이용하여 표면적,체적구하기 표면적4*pi*r^2 , 체적:4/3*pi*r^3
	return 0;
}
