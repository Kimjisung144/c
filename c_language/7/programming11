#include <stdio.h>

int main(void)
{
	int a;
	int sum=0;
	printf("n의 값을 입력하시오:");
	scanf_s("%d", &a);
	for (int i = 1; i <= a; i++)
	{
		sum = sum + (i * i);
	}
	printf("계산값은 %d입니다.", sum);
	return 0;
}
