#include <stdio.h>

int main(void)
{
	int a;// 최대 구하는 값
	while (1) 
	{
		printf("막대의 높이(종료-1):");
		scanf_s("%d", &a);
		if (a == -1)
		{
			break;
		}
		for (int i = 1; i <= a; i++)
		{
			printf("*");
		}
		printf("\n");
	}
	return 0;
}
