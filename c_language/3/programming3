/*사용자로 부터 삼각형의 높이와 밑변을 받아서 넓이를 계산하여 추력하는 프로그램을 작성하라. 단 모든데이터는 실수로 입력되며 출력도 모두 실수형으로 하여야 한다.*/
#include <stdio.h>
int main()
{
	float h, x1,a;
	printf("밑면을 입력하시오:");
	scanf_s("%f", &x1);
	printf("높이를 입력하시오:");
	scanf_s("%f", &h);
	a = (x1 * h) * 0.5;
	printf("삼각형의 넓이는 %f입니다.",a);
	return 0;
} 
