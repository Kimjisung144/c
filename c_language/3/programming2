/*미국에서는 거리를 표시하는데 마일을 사용한다.마일을 미터로 환산하는 프로그램을 만들어보자.사용자로부터 마일단위로 거리를 입력받아서 변수에 저장한다. 이변수에 1,609를 곱하여 미터로 변환한다.
미터로 변환된 값을 화면에 출력한다. 실수값을 사용한다.*/
#include <stdio.h>
int main()
{
	float mile, m;
	printf("마일을 입력하시오:");
	scanf_s("%f", &mile);
	m = mile * 1609;
	printf("%f마일은 %f미터입니다.",mile, m);
	return 0;
} 
