/*사용자로부터 세개의 실수를 입력받은후, 합계와 평균값을 계산하여 화면에 출력하는 프로그램을 작성하라*/
#include <stdio.h>
int main()
{
	float x1, x2, x3, sum, avg;
	printf("실수를 입력하시오:");
	scanf("%f", &x1);
	printf("실수를 입력하시오:");
	scanf("%f", &x2);
	printf("실수를 입력하시오:");
	scanf("%f", &x3);
	sum = x1 + x2 + x3;
	avg = (x1 + x2 + x3) / 3;
	printf("합은%f이고 평균은 %f입니다.", sum,avg);
	return 0;
} 
