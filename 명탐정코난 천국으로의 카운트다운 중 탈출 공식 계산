/*명탐정코난 천국으로의 카운트다운 중 탈출 공식 계산
일단 날아가는데 걸리는 시간을 구하기위해 
루트 (2*두건물간의 거리/ 중력의 가속도)
1초당 가야하는 거리를 구하기 위해
건물간의 거리 / 날아가는데 걸리는 시간
자동차 속도를 구하기위해
60*60 = 3600 = 시속
즉 1초당 날아가야하는 거리 * 3600
거기에 m에서 km로 바꾸기위해 /1000이기에
1초당 날아가야하는거리 *3600 / 1000
*/ 

#include<stdio.h>
#include <math.h>

int main(void)
{
	const double g = 9.80665; //중력 가속도 9.80665 m / s2
	double dis; //건물간의 거리
	double h; //건물간의 높이차
	double t; // 가는데 걸리는 시간(second)

	printf("건물간의 거리를 입력하세요:");
	scanf_s("%lf", &dis);
	printf("건물간의 높이차를 입력하세요:");
	scanf_s("%lf", &h);

	t = 2 * h / g; //루트(2*h/g)
	t = sqrt(t); //sqrt = 제곱근

	dis = dis / t; //1초당 거리
	dis = dis * 3600; // 1초에서 1시간당으로 바꾸기 위해
	dis = dis / 1000; // m를 km로 바꾸기 위해
	printf("%3.lfkm/h로 달려야 한다.", dis); //정수 3자리까지 출력
	return 0;
}
